{
  "name": "sonicpages",
  "version": "1.1.0",
  "private": true,
  "description": "A privacy-first, browser-based PDF reader with text-to-speech.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  }
}
